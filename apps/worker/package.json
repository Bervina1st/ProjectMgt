{
  "name": "@pmstatus/worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "echo \"(worker) deferred — see README\"",
    "typecheck": "echo \"(worker) deferred — see README\""
  }
}
